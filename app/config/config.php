<?php
  const DB_HOST = "db";
  const DB_PORT = 3306;
  const DB_NAME = "mysql";
  const DB_USER = "root";
  const DB_PASS = "admin";
  const DB_SOCK = "/var/run/mysqld/mysqld.sock";
  const BASE_URL = "http://localhost:8080";
  // If there is base path before the starting file, set this to remove it from
  // routing using str_replace
  const BASE_PATH = null;
  // This is the default index file for the website. This is typically index.php
  const BASE_FILE = "/index.php";